# Documentation

Reference docs for the Slay the Spire 2 (decompiled) codebase.

| Doc | What it covers |
|-----|----------------|
| [ARCHITECTURE.md](ARCHITECTURE.md) | Whole-project architecture: subsystems, layers, entry points, and where everything lives. |
| [card_trail_vfx.md](card_trail_vfx.md) | The card pile-movement trail effect — scripts and prefabs for the streak behind cards flying between piles (played → discard, discard → draw reshuffle). |

> Note: `ARCHITECTURE.md` here is a copy of the root-level `ARCHITECTURE.md`. The
> root copy is the one GitHub renders by default; its internal links are written
> relative to the repository root, so follow them from the root file.
